{
    "name": "lambda-auth-ts",
    "version": "1.0.0",
    "description": "Lambda function for authentication using Cognito (TypeScript)",
    "main": "dist/index.js",
    "scripts": {
      "build": "tsc src/index.ts",
      "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
      "test": "jest",
      "deploy": "npm run build && echo \"Deploy script not implemented\" && exit 1"
    },
    "keywords": ["lambda", "authentication", "cognito", "typescript"],
    "author": "",
    "license": "ISC",
    "dependencies": {
      "@aws-sdk/client-cognito-identity-provider": "^3.x.x"
    },
    "devDependencies": {
      "@types/aws-lambda": "^8.10.92",
      "@types/node": "^14.14.31",
      "ts-node-dev": "^1.1.6",
      "typescript": "^4.2.3"
    }
  }
  